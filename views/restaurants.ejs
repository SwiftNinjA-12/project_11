<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('include/head') %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <%- include('include/header') %>
    <%- include('include/aside') %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if(numberOfRestaurants === 0) { %>
      <p>
        Unfortunately, we have no restaurants yet - maybe start recommending
        some.
      </p>
      <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We found <%= numberOfRestaurants %> restaurants</p>
      <ul id="restaurants-list">
        <% for (const restaurant of restaurants){ %>
        <!-- writing JS code inside html  thanks to EJS. this open the for loop note single (})-->
        <%- include('include/Restaurants/restaurant-item', {restaurant:
        restaurant}) %> <% } %><!--this is the end of the for loop-->
      </ul>
      <% } %>
    </main>
  </body>
</html>
